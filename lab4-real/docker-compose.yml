version: '3.9'
services:
  builder:
    image: docsy-builder:latest
    volumes:
      - site:/site
    environment:
      PROJECT_NAME: "${PROJECT_NAME}"
      SPHINX_THEME: "${SPHINX_THEME}"
    command: /usr/src/app/docsy/.venv/bin/sphinx-build -b html /usr/src/app/docsy/docs/source /site
    networks:
      - docsy-net
    restart: "no"

  web:
    image: nginx:alpine
    volumes:
      - site:/usr/share/nginx/html:ro
      - ops:/etc/nginx/conf.d
    ports:
      - "${DOCS_PORT}:80"
    depends_on:
      builder:
        condition: service_completed_successfully
    networks:
      - docsy-net
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost/"]
      interval: 10s
      retries: 3
      
  dev:
    image: docsy-builder:latest
    working_dir: /usr/src/app/docsy/docs/source
    profiles: ["dev"]
    environment:
      BUILD_DIR: "/site"
    command: >
      sh -c "
        /usr/src/app/docsy/.venv/bin/sphinx-autobuild --host 0.0.0.0 --port 8000 . ${BUILD_DIR}"
    volumes:
      - site:/site
    ports:
      - "8000:8000"
    networks:
      - docsy-net

  dozzle:
    image: amir20/dozzle:latest
    profiles: ["obs"]
    ports:
      - "9000:8080"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    depends_on:
      web:
        condition: service_started
    networks:
      - docsy-net

  cadvisor:
    image: gcr.io/cadvisor/cadvisor:v0.49.1
    profiles: ["obs"]
    ports:
      - "9100:8080"
    volumes:
      - /:/rootfs:ro
      - /var/run:/var/run:ro
      - /sys:/sys:ro
      - /var/lib/docker/:/var/lib/docker:ro
    networks:
      - docsy-net
      
volumes:
  site:
  ops:

networks:
  docsy-net:
